class Solution 
{ 
    int fun(String s) 
    {
        // code here
        HashSet<String> h=new HashSet<>();
        for(int i=0;i<s.length()-1;i++){
            String re="";
            re=s.charAt(i)+""+s.charAt(i+1);
            h.add(re);
        }
        return h.size();
    }
} 
