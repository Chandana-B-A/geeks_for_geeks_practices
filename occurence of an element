class Solution
{
    public static int count(Node head, int key)
    {
        //code here
        int res=0;
        while(head!=null){
            if(head.data==key)
                res++;
            head=head.next;
        }
        return res;
    }
}
